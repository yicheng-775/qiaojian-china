# 桥见川渝 · 如何自主更新

本文写给「以后不靠 AI 也能自己改」的你。网站是**纯静态前端 + 一个 Netlify 云函数**，没有数据库、没有构建步骤——改完文件、提交、推送，Netlify 自动重新部署，几秒钟上线。

---

## 一、先记住两件最重要的事

1. **改了前端 JS/CSS，必须把版本号 `?v=N` 加 1**，否则浏览器会读缓存的旧文件，你会以为「改了没生效」。
   位置在 `index.html` 底部一堆 `<script src="js/xxx.js?v=N">`。只改 `index.html` 本身不用动版本号（它是入口页）。

2. **API key 永远只填在「⚙ 设置」里**（存在你浏览器 localStorage），**不要写进任何代码文件**——这个仓库是公开的。

---

## 二、常见改动，改哪个文件

| 想改什么 | 文件 | 怎么改 |
|---|---|---|
| **术语表**（专名中英对照） | `js/corpus.js` | 在 `glossary` 里加一行：`"中文": { en: "English", region: "重庆" }` |
| **生活核心词术语表** | `js/data.js` 的 `QJC.coreGlossary` | 同上格式 |
| **文化词词典**（带解释、高亮、翻译建议） | `js/data.js` 的 `QJC.dictionary` | 加一个 `{term, category, level, region, reason, suggestions}` 对象 |
| **示例文章**（首页那些 chip） | `js/data.js` 的 `QJC.cases` | 加一个 `{id, title, tag, tagClass, summary, source, targets}`；并在 `index.html` 的 `.examples` 里加一个 `<button class="chip" data-case="你的id">…</button>` |
| **AI 提示词**（翻译/改稿/分类/画像） | `js/prompts.js` | 直接改四段 system prompt 的文字 |
| **模型名 / 访问密码 / 温度 / 历史条数上限** | `js/config.js` | 改 `model`、`authPassword`、`temperature`、`historyLimit` 等 |
| **云函数（代理 DeepSeek、重试、超时）** | `netlify/functions/ai.js` | 谨慎改，见第四节 |

改完记得 bump 对应文件的 `?v=N`。

---

## 三、术语表详细格式（最常改）

`js/corpus.js` 里长这样：

```js
QJC.corpus = {
  glossary: {
    "吊脚楼":   { en: "Diaojiaolou (stilt house)", region: "巴蜀" },
    "湖广会馆": { en: "Huguang Guild Hall", region: "重庆" },
  }
};
```

- `en`：官方/通行英文译法，尽量带个简短注释（括号里）。
- `region`：`成都` / `重庆` / `巴蜀`（两城共赏）。这个值会写进提示词，帮 AI 别把川渝混为一谈。
- 这两个术语表（`corpus.js` + `data.js` 的 `coreGlossary`）**自动合并**进翻译/改稿提示词，不用再手动同步。

---

## 四、云函数 `netlify/functions/ai.js` 的几个坑（别重蹈）

- 用 **Node 内置 `https`** 模块，不是 `fetch`（Node 版本 <18 没有 fetch，会 502）。
- 请求体里必须带 `thinking: { type: "disabled" }`——deepseek-v4 默认开思考链，关了才能快返回。
- `req.setTimeout(8000)` 是**故意设 8 秒**：Netlify 免费版函数硬超时 10 秒，设大了会被平台强杀、返回非 JSON 的 500。
- 自动重试只针对「JSON 不完整」的偶发截断；超时场景直接放弃，不重试（重试也来不及）。

---

## 五、语料库 zip 再更新怎么办

新 `语料库资料.zip` 里的 14 组 CN/EN `.docx` 是「平行语料」。要吸收进来：

1. 解压，逐组看 `CN-x.x.docx` 和 `EN-x.x.docx` 里的**条目标题**（如 `01 山城巷 / Shancheng Lane`）。
2. 把有价值的「中文专名 → 英文」抄进 `js/corpus.js` 的 `glossary`（**注意：docx 里标题有时中文和英文挤在同一行、有时编号和正文粘在一起，要人工校订，别直接照搬**）。
3. 想新增示例文章，就抄一段干净的中文 + 英文，按 `QJC.cases` 的格式加到 `js/data.js`。
4. bump 版本号、提交、推送。

> 附：仓库外层有 `_extract_corpus.py`（`d:\UserData\Desktop\新文科大赛\` 下），可以批量把 docx 里的段落和标题抓出来做参考。但它只是辅助，**自动对齐偶尔会错位**（比如手工技艺、码头文化两组），最终仍要人眼校订。

---

## 六、提交 + 部署（一次性记住）

```bash
cd "d:/UserData/Desktop/新文科大赛/qiaojian-china"
git add -A
git commit -m "更新：xxx"
git push
```

- 站点连的是 GitHub 仓库 `yicheng-775/qiaojian-china`，**线上地址是 `https://spectacular-maamoul-41d2f6.netlify.app`**（不是 `qiaojian-china.netlify.app`）。
- push 后 Netlify 自动构建部署，等 1~2 分钟刷新即可。
- 验证云函数活着：浏览器打开 `https://spectacular-maamoul-41d2f6.netlify.app/.netlify/functions/ai`，应返回 `{"ok":true}`。

---

## 七、本地自测（不烧积分）

想确认前端改得对不对、又不想消耗 DeepSeek 积分：

```bash
cd "d:/UserData/Desktop/新文科大赛/qiaojian-china"
python -m http.server 8080
```

然后浏览器开 `http://localhost:8080`。**不填 key** 就是「兜底模式」（词典高亮 + 免费机翻），可以验证界面、模式切换、历史记录、上传/导出等所有不烧积分的功能。

要看 AI 真实效果，再用自己 key 在「⚙ 设置」里填一次、翻译一小段即可（每次翻译都会真实调 DeepSeek，按量计费，请控制测试量）。
